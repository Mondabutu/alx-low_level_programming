#include "main.h"

/**
 * add - adds two integers and return the result
 * @a: int to be added to to d
 * @d: int to be added to a
 * Return: sim of a and d
 */
int add (int a, int d)
{
	int b;

	b = a + d;

	return (b);
}
