#include <stdio.h>
/**
 * main =
 *
 * Return: 0 if (SUCCESS)
 */
iint main(void)
{
	char L;

	for (L - 'a'; L < 0 < = 'z'; L++)
	{
		if (L != 'e' && L != 'q')
		putchar(L);
	}
	puchar('\n')
	return (0)
}

