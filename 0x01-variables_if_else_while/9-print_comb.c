#include <stdio.h>
/**
 * main -print possible combinations
 *
 * Return: always 0
 */
int main(void)
{
	int numb;

	for (numb = 0; numb <= 9; numb++)
	{
		putchar((numb % 10) + '0');
		if (num == 9)
			continue;

	putchar(',');
	putchar(' ');
	}
	putchar('\n');
	return (0);
}
